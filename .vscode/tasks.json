{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Zip Package",
            "type": "shell",
            "command": "Compress-Archive -Path ${workspaceFolder}/package/* -DestinationPath ${workspaceFolder}/build/package.zip",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}